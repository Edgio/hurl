1. fix hle with edge triggered epoll
2. hlx_server.
  a. Memory management
  b. stats
  c. access logs
  d. http_req splitting -if read overlaps
  e. thread-safe q's
  f. disallow enqueue'ing multiple requests for the same conn
  g. Catch+unit tests
  h. blackbox tests
