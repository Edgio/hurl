1. hle
  a. fix hle with edge triggered epoll
  b. fix addr resolution errors showing up as "successful" results.

2. hlx_server.
  a. Memory management
  b. stats
  c. access logs
  d. http_req splitting -if read overlaps
  e. thread-safe q's
  f. disallow enqueue'ing multiple requests for the same conn
  g. Catch+unit tests
  h. blackbox tests
  i. fix hlo tests -connection errors and "Error: performing m_nconn_pool.get"
