0. hlx
  a. evr_kqueue + eventfd analog.
  b. fix stats stdev/var.
  c. async dns timeouts
  e. access logs???
  f. rqst splitting (test with pipelined requests)
  g. set separate timeouts resolve/connect/idle
  h. error callback indicate error type
  g. error responses for handlers

